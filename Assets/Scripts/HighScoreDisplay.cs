using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class HighScoreDisplay : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {// to ma być podpięte pod wyświetalnie aktualnego nicku do aktualnej ilosci pkt (chyba w GameManagerze mogę dodać) a wyświetlanie najlepszego wyniuku jeszcze się dorobi

    }

    // Update is called once per frame
    void Update()
    {
    }


}
